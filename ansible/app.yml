---
- name: Configure application
  hosts: app
  gather_facts: no
  become: true
  vars:
    db_node: "{{ hostvars[inventory_hostname_short]['dbserver'] }}"
    db_ip:   "{{ hostvars[inventory_hostname_short]['db_ip'] }}"
    db_host: "{{ hostvars[db_node]['internal_ip'] }}"
  tasks:
    - debug: var=db_node
    - debug: var=db_ip
    - debug: var=db_host

    - name: Add unit file for Puma
      copy:
        src: files/puma.service
        dest: /etc/systemd/system/puma.service
      notify: reload puma

    - name: Add config file for DB connection
      template: 
        src: templates/db_config.j2
        dest: /home/appuser/db_config
        owner: appuser
        group: appuser

    - name: enable Puma
      systemd: name=puma enabled=yes
      
  handlers:
  - name: reload puma
    systemd: name=puma state=restarted

